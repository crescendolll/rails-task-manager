<h1><%= @task.title %></h1>
<h3><%= @task.details %></h3>
    <% if @task.completed %>
    <p> task is completed</p>
    <% else  %>
    <p> task is not completed</p>
    <% end  %>
<%= link_to "Back to list", tasks_path %>
